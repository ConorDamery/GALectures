import "app" for App

class Main {
    static init() {
    }

    static update(dt) {
    }

    static render() {
    }

    static netcode(server, client, event, peer, channel, packet) {
    }

    static audio(sampleRate, dt) {
    }
}